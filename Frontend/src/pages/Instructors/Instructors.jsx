import React from 'react';

import ShowInstructors from './All/ShowInstructors';

const Instructors = () => {
    
    return (
        <section>
            <ShowInstructors />
        </section>
    );
};

export default Instructors;